datasource db {
    provider = "sqlite"
    url      = env("DB_URL")
}

model User {
    id       String    @id
    sessions Session[]
}

model Session {
    id        String   @id
    userId    String
    expiresAt DateTime
    user      User     @relation(references: [id], fields: [userId], onDelete: Cascade)
}
